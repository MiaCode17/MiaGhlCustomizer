VITE_API_BASE_URL=http://localhost:8080/api/v1

# Set to "true" to preview the UI with realistic sample data and no backend at all
# (requests are served in-memory by src/services/mockAdapter.ts). Set to "false"
# once MongoDB and the backend server are up and running.
VITE_USE_MOCKS=false
